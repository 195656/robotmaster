#  AI 辅助开发与 Debug 实验日志：四角星动态识别稳定性调优

## 1. 问题现状
 OpenCV 求解 PnP 以及多边形拟合的实时视频流代码时，静态下对“四角星”的识别率较高。但当目标物体**快速移动**或摄像头发生晃动时，画面会出现严重的闪烁，高频报错 `Detecting...`，导致识别极度不稳定。

## 2. 对话记录：AI 协助问题剖析
通过ai系统分析了快速移动导致跟丢的**底层视觉机理**：

1. **运动模糊（Motion Blur）破坏了几何刚性**：快速移动导致四角星边缘虚化，导致原代码中基于静态设计的硬性指标（如：必须正好 7~9 个顶点、宽高比严格在 0.7~1.3 之间）无法被满足。
2. **前置去噪“帮倒忙”**：原代码采用了 `(5, 5)` 的高斯核，加上后面的 `MORPH_OPEN`（开运算，先腐蚀后膨胀）。在运动模糊发生时，腐蚀操作直接切断了星星变淡、变薄的触角，使其在二值化图里退化为圆块。
3. **策略缺乏时序弹性**：每一帧都是独立的“平行宇宙”。原代码直接根据单帧结果判定 `len(filtered_contours) > 0`，只要有一帧因为晃动没认出来，就立刻清空所有标记并报“跟丢”。

---

## 3. Debug 调优日志（迭代过程）

针对上述痛点，在 AI 的协助下，对代码进行了以下几轮参数与逻辑的重构（Debug 迭代过程）：

###  针对应对运动模糊（图像预处理调优）
* **修改前**：固定阈值二值化（`cv2.threshold` 125） + 大高斯模糊（5x5）。
* **修改后**：将高斯核降级为 `(3, 3)` 甚至不加，最大化保留边缘；引入 `cv2.adaptiveThreshold`（自适应二值化），根据局部明暗自动决定阈值，极大增强了动态环境下的抗噪性。
* **执行效果**：移动时变虚的星星触角成功被二值化捕获。

###  针对形态学策略转变
* **修改前**：`cv2.MORPH_OPEN`（先腐蚀后膨胀）。
* **修改后**：调整为 `cv2.MORPH_CLOSE`（闭运算，先膨胀后腐蚀）。
* **执行效果**：强行粘合了因为快速移动、残影产生的空隙和断裂处，保护了四角星的整体骨架。


